#
# libtsm
# ------
#
# The following imported targets are created:
#     libtsm::libtsm
#

@PACKAGE_INIT@

set_and_check(libtsm_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(libtsm_LIBRARY_DIR "@PACKAGE_LIBRARY_INSTALL_DIR@")

if(NOT TARGET libtsm::tsm)
    include("${CMAKE_CURRENT_LIST_DIR}/libtsm-targets.cmake")
endif()
